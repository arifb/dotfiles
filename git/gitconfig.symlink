[user]
  name = Arif Bandali
	email = hello@arifb.com
[color]
  diff = auto
  status = auto
  branch = auto
[core]
  excludesfile = ~/.gitignore
  # ignoring this line as
  # vim segfaulting in os x
  # don't really care as use
  # macvim, even in terminal,
  # and fixing the system vim
  # is going to be a pain
  #editor = vim
[apply]
  whitespace = nowarn
[mergetool]
  keepBackup = false
